import { initAddressHandling } from "./quote-address-handling";

document.addEventListener("DOMContentLoaded", function () {
    // Initialize address handling functionality (without form reset)
    initAddressHandling();
});
